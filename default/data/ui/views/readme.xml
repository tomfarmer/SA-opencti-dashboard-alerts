<dashboard version="1.1" theme="dark">
  <label>README</label>
  <description>Overview, usage, and configuration for the OpenCTI Threat Match Dashboard app.</description>

  <row>
    <panel>
      <html>
        <pre style="white-space:pre-wrap; font-family: SFMono-Regular,Consolas,Monaco,monospace; padding:16px; max-width:980px;">
# OpenCTI Threat Match Dashboard

A Splunk app that enriches your network telemetry with OpenCTI indicators of compromise (IOCs), writes normalized summary events to `index=opencti_alerts`, and provides analyst‑friendly dashboards to triage, label, and revisit matches.

## About This App
- Purpose: correlate OpenCTI IOCs with DNS, IP/flow, URL/HTTP, and file events; summarize matches; and accelerate analyst review.
- Scope: includes scheduled matchers, backfills, KV lookups, workflow actions, and dashboards (Analyst and Health).

## How The App Works
- Data flow
  - OpenCTI → Splunk: an OpenCTI “Live Stream”/connector populates an upstream lookup (e.g., `opencti_lookup`).
  - This app refreshes purpose‑built KV stores from that lookup:
    - Domains: `opencti_domains_kv` via transform `opencti_lookup_domain` (match_type=WILDCARD(domain))
    - IP/CIDR: `opencti_ips_kv` via transform `opencti_lookup_ip` (match_type=CIDR(ip))
    - Refresh searches intentionally read the upstream `value` field only; `name` is not used as a fallback IOC source.
  - Scheduled matchers read enabled rows from the Correlations UI KV collection (`opencti_tm_monitored_indexs_and_fields`) and enrich with those KV stores:
    - Domains, IPs, URLs, hashes, and emails all use configured `indexes` and `fields` from the KV collection.
    - Basic searches treat missing or empty `correlation_mode` as `basic` for compatibility with older saved rows.
    - DNS answer IPs are handled by adding an enabled Basic IP row such as `indexes=zeek_dns`, `fields=answers`.
  - Matches are summarized and written to `index=opencti_alerts` (macro `opencti_alerts_index`) by `collect`.
  - Summary index model: `opencti_alerts` is a summary index. Each matcher aggregates raw events (stats … BY indicator) and writes a single summary row per run window carrying `hits`, `first_seen`, `last_seen`, and sets `_time=last_seen`. You will not see one summary event per raw hit; this keeps the index small and dashboards fast.

- Fields written to `index=opencti_alerts`
  - From OpenCTI KV lookups (`opencti_lookup_domain`, `opencti_lookup_ip`, `opencti_lookup_url`, `opencti_lookup_hash`, `opencti_lookup_email`):
    - `ioc` (original IOC name/value)
    - `score`
    - `labels`
    - `created_by`
    - `confidence`
  - From telemetry/aggregation in this app:
    - Common: `hits`, `first_seen`, `last_seen`, `src_index`, `src_sourcetype`, `kind`, `_time` (set to `last_seen`), `alert_summary`
    - Domain matches: `match_value`
    - IP matches: `ip`, `correlation_name`
    - URL matches: `url_candidate`
    - File matches: `hash`
    - Email matches: `email`

- Saved searches (matchers &amp; backfills)
  - Near‑real‑time (5 min):
    - `domains_match_opencti` (aggregates unique domains before lookup for efficiency)
    - `ip_match_opencti`
    - `url_match_opencti`
    - `file_hash_match_opencti`
    - `email_match_opencti`
  - Backfills (ad‑hoc or scheduled off):
    - `backfill_domain_match_opencti`
    - `backfill_ip_match_opencti`
    - `backfill_url_match_opencti`
    - `backfill_file_hash_match_opencti`
    - `backfill_email_match_opencti`

- Handling IOC drift over time
  - Backfills re‑scan historical telemetry against the current IOC sets, capturing matches for indicators added or changed after events were first ingested.
  - The Splunk Search time picker controls which source events are scanned during a backfill.
  - Summary `_time` is set to `last_seen` from the matched source event, not the wall-clock time when the backfill ran. This keeps dashboards aligned to when the activity happened; to validate fresh writes, search by `_indextime`.

- “Seen again” + “Cleared” logic on the Analyst dashboard
  - The dashboard looks up analyst decisions from `opencti_seenbefore_kv` (via `opencti_seenbefore_kv` lookup), then evaluates `seen_again` as Yes/No based on whether a new `last_seen` is later than `decided_at`.
  - Workflow actions let analysts set `ioc_rating` (malicious/benign/unreviewed), a separate `cleared` flag (0/1), `decided_at`, `decided_by`, and `note` by writing/upserting into `opencti_seenbefore_kv`.
  - Semantics:
    - `ioc_rating` = classification of the IOC (malicious/benign/unreviewed).
    - `cleared=1` = this IOC’s matches have been reviewed and handled (case closed), even if the IOC remains malicious.
    - `cleared=0` (or null) = not yet cleared; still open from an operational point of view.

## How To Use
- Dashboards
  - Analyst Overview (`default/data/ui/views/opencti_overview.xml`)
    - Summary tiles for flagged Files/Domains/URLs/IPs.
    - “Domain/IP/URL/Email/File Matches” tables show: Rating, Indicator, Low Score, Low Score By, Max Score, Max Score By, Score Range, Times Seen, Tags, Created By, Confidence, First/Last Time Seen, Index.
    - Click a row to populate action buttons (set IOC rating), show the source index, and open a ±5s raw‑event drilldown.
    - “Rolling … Matches” tables show today’s matches; use the column picker to show `created_by`/`confidence` if hidden.
  - OpenCTI Threat Match Health (`default/data/ui/views/opencti_health.xml`)
    - KV inventory, scheduler status, and summary‑event counts over time.

- Marking indicators (workflow actions)
  - On the Analyst dashboard row click, use buttons to mark Malicious/Benign/Clear. Actions write to `opencti_seenbefore_kv`; decisions immediately influence Rating and “Seen again”.

- Running backfills
  - Use the "Run Backfill" action on a correlation row, then narrow the Search time picker before running it.
  - If invoking a map-based saved search manually with `| savedsearch`, include `nosubstitution=true` so Splunk does not consume the `$field$` tokens used by `map`.
  - If you need immediate visibility for a backfill you just ran, search by `_indextime`; a “Last 15 minutes” `_time` search only shows matches whose source events happened in the last 15 minutes.
  - Fresh-write validation example: `index=opencti_alerts earliest=-48h | where _indextime&gt;=relative_time(now(), "-15m")`.

## Configuration &amp; Customization
- Macros you can tune
  - `opencti_alerts_index`: target summary index (default: `opencti_alerts`)
  - `opencti_tm_index_time_gate_window`: index-time slice used by near-real-time matchers.
  - Source indexes and candidate fields should normally be changed in the Correlations UI, not in `macros.conf`.

- Lookups &amp; transforms
  - `opencti_lookup_domain` → `opencti_domains_kv` (WILDCARD(domain)) includes: domain, score, labels, created_by, confidence.
  - `opencti_lookup_ip` → `opencti_ips_kv` (CIDR(ip)) includes: ip, score, labels, created_by, confidence.
  - Threat‑feed union lookups (`opencti_threatfeed_current_*`) also expose `feed_scores` (MV of `feed:score`), `max_score`, `min_score` (rendered as “Low Score” in the dashboard), and `score_range`.
  - Analyst decisions: `opencti_seenbefore_kv` (fields: _key, indicator, type, ioc_rating, cleared, decided_at, decided_by, note).

## Wildcards and matching
  - Domains
    - We normalize to lowercase and strip any trailing dot, then store two patterns for each IOC: `domain.com` and `*.domain.com`.
    - The transform `opencti_lookup_domain` uses `match_type=WILDCARD(domain)`, so it matches the apex and any subdomain (for example `a.b.domain.com`).
  - URLs
    - We strip URL schemes, lowercase, and store the host/path in `opencti_urls_kv`. The transform `opencti_lookup_url` uses `match_type=WILDCARD(url)`.
    - This allows patterns like `example.com/*` and `*.example.com/*` to match across path segments and subdomains. Extraction builds candidates as `host.uri` or `host.uri_path` when available.
  - File hashes
    - Hash matching is exact by algorithm. An MD5 or SHA1 seen in telemetry will not match a SHA256-only OpenCTI IOC unless the same SHA256 is also present in telemetry or OpenCTI has the MD5/SHA1 IOC too.
    - If your OpenCTI feed is SHA256-only, configure the source field as `sha256`. Include `md5` or `sha1` only when your OpenCTI hash KV also contains those algorithms.
  - IPs
    - We use network-aware matching via `match_type=CIDR(ip)` so `10.0.0.0/8` matches `10.1.2.3` (both IPv4 and IPv6 supported).
    - Public-only IP matching drops private and non-routable ranges before lookup, including RFC1918, loopback, link-local, CGNAT, multicast, reserved, and IPv6 ULA/multicast space.
  - Why we do it this way
    - Correctness: Wildcards avoid blind spots (e.g., subdomains) without enumerating all permutations; CIDR matches ranges as intended.
    - Performance: We pre-aggregate unique candidates (domain/url) before looking them up to cut KV scans by orders of magnitude.
    - Predictability: Normalization (lowercasing, scheme/trailing dot removal) makes the matching behavior deterministic across sources.

## Packaging
  - App label: “OpenCTI Threat Match Dashboard”; dashboard title: “Analyst Overview”.
  - App icon/logo: place `appserver/static/appIcon.png` (48×48) and `appserver/static/appLogo.png` (≈72×72). Optional dark variants: `appIconAlt.png`, `appLogoAlt.png`.
  - Build script: `scripts/build.sh` produces a tarball that unpacks as `SA-opencti-threat-match-dashboard`.

## Notes &amp; Tips
- Efficiency: The DNS matcher aggregates unique domains before wildcard lookups to reduce KV operations.
- Time semantics: Dashboards use `_time` (set to `last_seen`), while ingestion monitoring is easier with `_indextime`.
- Troubleshooting
  - If matchers run but dashboards show blanks for Created By/Confidence, ensure the refreshers have populated those fields into the KV stores and that the saved searches include them before `collect`.
  - If you see `[map]: Field 'ip_candidates' does not exist in the data.`, it typically means either (a) there were no events in the gated `_indextime` window (`m_index_time_gate` defaults to the last 5 minutes), or (b) none of the configured IP fields exist for that sourcetype. You can widen the default gate by editing `opencti_tm_index_time_gate_window` in `default/macros.conf`, or adjust the `fields` column in `opencti_tm_monitored_indexs_and_fields`.
  - Private IPs: IP matchers in this app can be configured to include or exclude RFC1918/link-local/loopback. See `m_otm_ip_valid_any` vs `m_otm_ip_valid_public` in `default/macros.conf`.
  - `maxsearches=50`: this is the `map` command limit (caps how many per-row searches Splunk will spawn). If you have &gt;50 enabled rows in `opencti_tm_monitored_indexs_and_fields`, increase it.
  - Verify macros resolve as expected: `| makeresults | eval idx="`opencti_alerts_index`" | table idx`.

---
## Permissions &amp; Setup (Least Privilege Guide)

This app is designed to run with a dedicated, non-administrative role (`opencti_user`). The necessary permissions are defined in configuration files to ensure they are persistent and correct.

**1. Role &amp; Object Permissions (Configuration-Managed)**
   - **Role Definition:** The `opencti_user` role is defined in `local/authorize.conf`. It is granted basic search capabilities and access to the `opencti_alerts`, `_internal`, and `zeek*` indexes.
   - **Object Sharing:** All app objects (dashboards, searches, lookups) are shared with the app via `metadata/local.meta`. This file also grants the `opencti_user` role read access to dashboards/searches and write access to the KV Store lookups for analyst workflow actions.
   - These files ensure the app's components are owned by the app, not "nobody", and that permissions persist across restarts.

**2. Assign Role to Users (Manual Step)**
   - The only manual step is to assign the pre-configured `opencti_user` role to your security analysts.
   - Navigate to **Settings > Users**.
   - Assign the `opencti_user` role to your security analysts who will be using this app.

This file-based configuration is the most robust method and ensures the app functions correctly with least-privilege access.

---
If you need additional data sources or fields, add rows in the Correlations UI. Extend macros and saved searches only when adding a new matching behavior.
       </pre>
     </html>
   </panel>
 </row>
</dashboard>
